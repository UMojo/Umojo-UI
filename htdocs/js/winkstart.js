(function(winkstart, amplify, undefined) {
	winkstart.module = {
		// 
		define: function(module, definition) {
		},

		// 
		using: function(module, callback) {
		},

		// Method for 
		error: function(callback) {
		}
	};

})(	window.winkstart = window.winkstart || {},
	window.amplify = window.amplify || {});
